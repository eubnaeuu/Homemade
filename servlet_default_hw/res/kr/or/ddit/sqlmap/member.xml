<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<select id="selectMemberByID" parameterType="str" resultType="memvo">

		SELECT
			ID 			 id
			,PWD         pwd
			,EMAIL       email
			,PICTURE     picture
			,ENABLED     enabled
			,REGDATE     regdate
			,PHONE       phone
			,NAME        name
			,REGISTER    register
			,ADDRESS     address
			,AUTHORITY   authority
		FROM MEMBER
        WHERE 1=1
        AND ID = #{id}
	</select>
	
	<select id="selectMember" resultType="memvo">
		SELECT
			ID 			 id
			,PWD         pwd
			,EMAIL       email
			,PICTURE     picture
			,ENABLED     enabled
			,REGDATE     regdate
			,PHONE       phone
			,NAME        name
			,REGISTER    register
			,ADDRESS     address
			,AUTHORITY   authority
		FROM MEMBER
        WHERE 1=1
	</select>
</mapper>